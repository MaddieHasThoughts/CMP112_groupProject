using NUnit.Framework;
using UnityEngine;
using System.Collections.Generic;

public class itemManager : MonoBehaviour
{
    private int artifactCounter; //count how many artifacts are on screen to max out at a certain number

    void Start()
    {
        
    }

    void Update()
    {
        if (artifactCounter < 7)
        {
            List<int> previousSpawnLocations = new List<int>();

            //Finish when theres a background

        }



    }
}
